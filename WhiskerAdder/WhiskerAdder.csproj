<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net6.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>
    <ItemGroup Condition=" '$(EnableDefaultItems)' == 'false' ">
    <Compile Include="Bash.cs" Condition=" '$(EnableDefaultCompileItems)' == 'false' " />
    <Compile Include="IconTool.cs" Condition=" '$(EnableDefaultCompileItems)' == 'false' " />
    <Compile Include="PathTool.cs" Condition=" '$(EnableDefaultCompileItems)' == 'false' " />
    <Compile Include="Program.cs" Condition=" '$(EnableDefaultCompileItems)' == 'false' " />
    </ItemGroup>

</Project>
